<% if !@artist.photo.nil? %>
  <%= image_tag @artist.photo.image %>
<% end %>
<% @artist.songs.each do |song| %>
  <div class="row">
    <div class="col-md-7">
      <h4><%= song.name %></h4>
    </div>
  </div>
<% end %>
